const useNuxtinPerks = () => {
	const points = ref(null)
	const offers = ref(null)
	const isOffer = ref(false)


	const checkPoints = (data) => {
		
	}

	return {
		points,
		offers,
		isOffer,
		checkPoints
	}
}